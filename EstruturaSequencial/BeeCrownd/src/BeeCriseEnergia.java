import java.util.Scanner;
import java.util.List;
import java.util.ArrayList;
public class BeeCriseEnergia {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		Integer n = null;
		while(n != 0) {
			n = sc.nextInt();
			List<Integer> regioes = new ArrayList<>();
			List<Integer> regioes_desligadas = new ArrayList<>();
			for(int i = 1; i <= n; i++) {
				regioes.add(i);
			}
			for(int j = 0; j < regioes.size(); j++) {
				
			}
		}
		sc.close();
	}

}
